defmodule Dobar.Dialog.CancelCommandDialog do
  use Dobar.Dialog.Species

  # def handle_intent(intent, %{topic: nil} = state) do
  #    IO.puts "x1 #{inspect self} begin topic: #{inspect intent}"
  #   {:ok, topic} = Dobar.Conversation.Topic.start_link(intent)
  #   {:topic_output, {"bla bla bla", %{}}}
  # end
end
