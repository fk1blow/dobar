defmodule Dobar.Timeline do
@moduledoc """
Dobar Timeline Api - dialog reactions store

This api should handle the conversational timeline of the Dialog System.
It has the property of a store which is specifically modeled to work with the
Dialog System, by using the Conversation Api

When something happened in the course of a dialog, a "spin" is to be registered
inside the timeline store - a record when some reaction is being triggered!
"""
end
